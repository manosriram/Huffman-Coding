#ifndef DEC
#define DEC
#include "HuffStruct.hpp"
bool taken = false;

string Decode(MinHeap *root) {

    string decoded = "";
    file.open("out.txt", ios::out | ios::binary | ios::in);
    MinHeap *Node = root;
    
    


}
#endif
